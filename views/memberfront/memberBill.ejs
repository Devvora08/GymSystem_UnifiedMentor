<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        /* General Transition for Links and Buttons */
        a,
        button {
            transition: all 0.3s ease;
            /* Smooth transition for hover effects */
        }

        /* Navbar Styles */
        ul.navbar {
            list-style-type: none;
            /* Remove default list style */
            padding: 0;
            /* Remove default padding */
            margin: 0;
            /* Remove default margin */
            display: flex;
            /* Use flexbox for horizontal layout */
            justify-content: space-around;
            /* Space out items evenly */
            background-color: #2c2c2c;
            /* Dark background for the navbar */
            border-radius: 5px;
            /* Round corners */
            padding: 10px;
            /* Padding around navbar */
        }

        ul.navbar li {
            display: inline;
            /* Inline display for list items */
        }

        /* Button Styles */
        

        button:hover {
            background-color: #81c7ff;
            /* Light blue background on hover */
            color: #fff;
            /* Change text to white on hover */
            transform: scale(1.05);
            /* Slightly enlarge the button */
        }

        /* Navbar Links Hover Effect */
        ul.navbar a {
            color: #818181;
            /* Default link color */
            text-decoration: none;
            /* Remove underline */
            padding: 10px;
            /* Padding for links */
        }

        ul.navbar a:hover {
            color: #ffffff;
            /* Change text color on hover */
            text-shadow: 0 0 10px rgba(255, 255, 255, 0.8);
            /* Add a glow effect */
        }

        /* Table Styles */
        table {
            width: 100%;
            /* Make the table full width */
            border-collapse: collapse;
            /* Collapse borders to avoid double borders */
            margin: 20px 0;
            /* Add margin for spacing */
        }

        th,
        td {
            padding: 12px;
            /* Add padding for cell content */
            text-align: left;
            /* Align text to the left */
            border: 1px solid #dddddd;
            /* Light gray border */
        }

        th {
            background-color: #f2f2f2;
            color: #1a1a1a;
            /* Light gray background for header */
        }

        tbody tr:hover {
            background-color: rgba(255, 255, 255, 0.1);
            /* Light hover effect */
            transform: translateY(-2px);
            /* Slightly lift the row */
            transition: transform 0.3s ease;
            /* Smooth transition for the row lift */
        }

        tbody tr:nth-child(even) {
            background-color: #f9f9f9;
            /* Slightly different color for even rows */
        }

        /* Member Details Section Animation */
        .member-details {
            opacity: 0;
            /* Start hidden */
            transform: translateY(-20px);
            /* Move up slightly */
            animation: fadeInUp 0.5s forwards;
            /* Fade in animation */
        }

        @keyframes fadeInUp {
            to {
                opacity: 1;
                /* Fully visible */
                transform: translateY(0);
                /* Move back to original position */
            }
        }

        /* Table Header Animation */
        th {
            opacity: 0;
            /* Start hidden */
            transform: translateY(-10px);
            /* Move up slightly */
            animation: fadeInDown 0.5s forwards;
            /* Fade in animation */
        }

        @keyframes fadeInDown {
            to {
                opacity: 1;
                /* Fully visible */
                transform: translateY(0);
                /* Move back to original position */
            }
        }

        /* Apply the header animation delay */
        th:nth-child(1) {
            animation-delay: 0.1s;
        }

        th:nth-child(2) {
            animation-delay: 0.2s;
        }

        th:nth-child(3) {
            animation-delay: 0.3s;
        }

        th:nth-child(4) {
            animation-delay: 0.4s;
        }

        th:nth-child(5) {
            animation-delay: 0.5s;
        }

        /* Apply the table body rows animation */
        tbody tr {
            opacity: 0;
            /* Start hidden */
            transform: translateY(10px);
            /* Move down slightly */
            animation: fadeInUp 0.5s forwards;
            /* Fade in animation */
        }

        tbody tr:nth-child(odd) {
            animation-delay: 0.5s;
            /* Add delay for odd rows */
        }

        tbody tr:nth-child(even) {
            animation-delay: 0.6s;
            /* Add delay for even rows */
        }

        /* Body Background */
        body {
            background: linear-gradient(to bottom right, #1a1a1a, #00695c);
            /* Example: Deep Ocean */
            color: #fff;
            /* Text color */
            font-family: Arial, sans-serif;
            /* Font style */
            margin: 0;
            /* Remove default margin */
            padding: 20px;
            /* Add padding */
        }
    </style>


</head>

<body>
    <ul class="navbar">
        <li><a href="/member/details">Details</a></li>
        <li><a href="/member/home">Home</a></li>
        <li><a href="/member/notification">Notification</a></li>
        <li><a href="/member/shop">Shop</a></li>
        <li><a href="/member/cancelmember">Cancel Membership</a></li>
    </ul>
    <br>
    <div class="member-details">
        <h2>Member Details</h2>
        <p><strong>Name:</strong>
            <%= user.name %>
        </p>
        <p><strong>Membership Plan:</strong>
            <%= memberDetails.membershipPlan %>
        </p>
        <p><strong>Membership Duration:</strong>
            <%= memberDetails.planDuration %>
        </p>
        <p><strong>Mobile Number:</strong>
            <%= memberDetails.mobileNumber %>
        </p>
        <p><strong>Email:</strong>
            <%= user.email %>
        </p>
    </div>

    <!-- Bills Section -->
    <h2>Your Bills</h2>
    <table>
        <thead>
            <tr>
                <th>Bill ID</th>
                <th>Amount</th>
                <th>Status</th>
                <th>Due Date</th>
                <th>Created At</th>
            </tr>
        </thead>
        <tbody>
            <% if (bills.length> 0) { %>
                <% bills.forEach(function(bill) { %>
                    <tr>
                        <td>
                            <%= bill._id %>
                        </td>
                        <td>Rs.<%= bill.amount %>
                        </td>
                        <td>
                            <%= bill.status %>
                        </td>
                        <td>
                            <%= new Date(bill.dueDate).toLocaleDateString() %>
                        </td>
                        <td>
                            <%= new Date(bill.createdAt).toLocaleDateString() %>
                        </td>
                    </tr>
                    <% }) %>
                        <% } else { %>
                            <tr>
                                <td colspan="5">No bills found.</td>
                            </tr>
                            <% } %>
        </tbody>
    </table>
</body>

</html>