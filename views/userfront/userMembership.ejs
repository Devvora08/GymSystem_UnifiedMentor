<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Select Plans</title>
    <style>
        body {
            background: radial-gradient(circle, #ffffff 15%, #240f2e 100%);
            color: #e0e0e0;
            font-family: 'Arial', sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: flex-start;
            height: 100vh;
            margin: 0;
            position: relative;
        }

        nav {
            width: 100%;
            padding: 10px 0;
            position: absolute;
            /* Fix navbar position */
            top: 0;
            /* Position navbar at the top */
            left: 0;
            /* Align to left */
            z-index: 10;
            /* Ensure navbar stays above content */
        }

        nav ul {
            display: flex;
            list-style: none;
            justify-content: center;
            padding: 0;
            margin: 0;
            
        }

        nav ul li {
            margin: 0 15px;
        }

        nav button {
            background: none;
            /* No background for buttons */
            color: #000000;
            /* Light text color */
            border: none;
            /* No border */
            cursor: pointer;
            /* Pointer cursor */
            font-size: 16px;
            font-weight: bold;
            /* Font size */
            transition: color 0.3s;
            /* Smooth color transition */
        }

        nav button:hover {
            color: #8a2be2;
            /* Change color on hover */
        }

        h1 {
            margin-top: 80px;
           color: #000000;
            text-align: center;
        }

        .form-container {
            margin-top: 20px;
            opacity: 0;
            /* Initially hidden for transition */
            transform: translateY(30px);
            /* Move down for transition */
            transition: opacity 0.5s ease-in-out, transform 0.5s ease-in-out;
            /* Transition for smooth entry */
            display: flex;
            /* Center items */
            flex-direction: column;
            /* Stack vertically */
            align-items: center;
            /* Center align */
            width: 100%;
            /* Take full width */
        }

        .plans {
            display: flex;
            justify-content: center;
            margin-top: 20px;
            flex-wrap: wrap;
            /* Allow wrapping to new line */
        }

        .plan-card {
            background-color:  #211a24;
            border: 1px solid #ccc;
            padding: 20px;
            margin: 10px;
            border-radius: 8px;
            text-align: center;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.3);
            width: 250px;
            /* Fixed width for all cards */
            height: 350px;
            /* Fixed height for all cards */
            transition: transform 0.3s, border 0.3s;
            cursor: pointer;
            /* Change cursor to pointer */
        }

        .plan-card:hover {
            transform: scale(1.05);
            /* Scale effect on hover */
        }

        .plan-card.selected {
            border: 2px solid #8a2be2;
            /* Highlight selected plan */
            transform: scale(1.05);
            /* Slightly enlarge selected card */
        }

        .plan-logo {
            width: 100px;
            height: auto;
            margin-bottom: 10px;
        }

        .highlight {
            color: #8a2be2;
            /* Highlight color for specific text */
            font-weight: bold;
        }

        input[type="radio"] {
            display: none;
            /* Hide default radio buttons */
        }

        input[type="radio"]:checked+.plan-card {
            border: 2px solid #8a2be2;
            /* Highlight selected plan */
            transform: scale(1.05);
            /* Slightly enlarge selected card */
        }

        input[type="radio"]:checked+.plan-card:hover {
            transform: scale(1.05);
            /* Prevent scale effect on hover when selected */
        }

        .age-input {
            margin: 20px;
            padding: 10px;
            border-radius: 5px;
            background-color: #211a24;
            border: 1px solid #ccc;
            /* Matching age input background */
            color: #e0e0e0;
            width: 200px;
            /* Fixed width for input */
            text-align: center;
            /* Center text */
        }

        .submit-button {
            background-color: #211a24;
            color: #fff;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s, transform 0.3s;
            margin-top: 20px;
            width: 200px;
            /* Fixed width for button */
            position: relative;
            left: 43.5%;
            margin-bottom: 15vh;
            text-align: center;
            justify-content: center;
            /* Center text */
            align-self: center;
            /* Center align button */
        }

        .submit-button:hover {
            background-color: #8a2be2;
            transform: scale(1.05);
        }
    </style>
</head>

<body>
    <nav>
        <ul>
            <li><button onclick="window.location.href='/user/plans'">Plans</button></li>
            <li><button onclick="window.location.href='/user/home'">Home</button></li>
            <li><button onclick="window.location.href='/user/store'">Shop</button></li>
        </ul>
    </nav>
    <h1>Select Your Plan</h1>
    <div class="form-container" id="formContainer">
        <form id="membershipForm">
            <label for="age" style="position: relative; left: 40%; color: #000000; font-weight: bold;">Enter your Age:</label>
            <input type="number" id="age" class="age-input" style="position: relative; left: 40%;" required
                placeholder="Age" />

            <div class="plans">
                <% allPlans.forEach((plan, index)=> { %>
                    <label>
                        <input type="radio" name="plan" value="<%= plan.planName %>" required />
                        <div class="plan-card">
                            <img src="<%= plan.planLogoUrl %>" alt="<%= plan.planName %> Logo" class="plan-logo">
                            <h2 class="highlight">
                                <%= plan.planName %>
                            </h2>
                            <p><strong>Duration:</strong> <span class="highlight">
                                    <%= plan.planDuration %>
                                </span></p>
                            <p><strong>Price:</strong> <span class="highlight">₹<%= plan.planPrice %></span></p>
                            <p>
                                <%= plan.description %>
                            </p>
                        </div>
                    </label>
                    <% }) %>
            </div>

            <button type="submit" class="submit-button">Choose Membership</button>
        </form>
    </div>

    <script>
        window.onload = function () {
            const formContainer = document.getElementById('formContainer');
            formContainer.style.opacity = 1; // Fade in
            formContainer.style.transform = 'translateY(0)'; // Slide up
        }

        // Optional: Add event listener to handle form submission
        document.getElementById('membershipForm').addEventListener('submit', function (event) {
            event.preventDefault();
            // Add your form submission logic here
            alert('Membership selected!');
        });
    </script>
</body>

</html>