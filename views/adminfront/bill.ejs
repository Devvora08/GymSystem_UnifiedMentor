<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bills</title>
    <style>
        body {
            /* Radial gradient background */
            background: radial-gradient(circle, rgba(0, 50, 0, 1) 0%, rgba(0, 0, 0, 1) 100%);
            color: #fff; /* White text for better contrast */
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 20px;
        }

        h1 {
            text-align: center;
            margin-bottom: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
            font-size: 16px;
            background-color: rgba(255, 255, 255, 0.1); /* Semi-transparent background for table */
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
            animation: fadeIn 1s ease forwards; /* Animation on load */
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        th,
        td {
            padding: 15px 20px;
            text-align: left;
        }

        th {
            background-color: #2e7d32; /* Dark green for headers */
            color: white;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }

        td {
            color: #ddd; /* Light text color */
            border-bottom: 1px solid rgba(255, 255, 255, 0.2); /* Light border */
        }

        /* Add alternate row color for better readability */
        tr:nth-child(even) {
            background-color: rgba(255, 255, 255, 0.05);
        }

        /* Row hover effect */
        tr:hover {
            background-color: rgba(255, 255, 255, 0.1);
            transition: background-color 0.3s ease-in-out;
        }

        /* Add some style to Amount and Status columns */
        td.amount {
            font-weight: bold;
            color: #4CAF50;
        }

        td.status {
            font-weight: bold;
            color: white;
            text-align: center;
            border-radius: 20px;
            padding: 5px 10px;
        }

        .status.paid {
            background-color: #4CAF50;
        }

        .status.unpaid {
            background-color: #e63946;
        }

        @media (max-width: 768px) {
            table,
            thead,
            tbody,
            th,
            td,
            tr {
                display: block;
                width: 100%;
            }

            th {
                display: none;
                /* Hide table headers */
            }

            td {
                display: block;
                padding: 10px;
                text-align: right;
                position: relative;
            }

            td:before {
                content: attr(data-label);
                position: absolute;
                left: 0;
                padding-left: 10px;
                font-weight: bold;
                text-transform: uppercase;
            }

            td.status {
                text-align: left;
            }
        }

        /* Button style */
        button {
            padding: 10px 15px;
            background-color: #4CAF50;
            color: white;
            position: relative;
            left: 47%;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            margin-top: 20px;
        }

        button:hover {
            background-color: #45a049;
        }

        /* Animation for table rows */
        .fadeInRow {
            opacity: 0;
            animation: fadeInRow 0.5s forwards;
        }

        @keyframes fadeInRow {
            from {
                opacity: 0;
                transform: translateY(10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>

<body>
    <h1>Bills</h1>
    <table border="1">
        <thead>
            <tr>
                <th>Name</th>
                <th>Email</th>
                <th>Age</th>
                <th>Mobile Number</th>
                <th>Joined At</th>
                <th>Membership Plan</th>
                <th>Duration</th>
                <th>Amount</th>
                <th>Status</th>
            </tr>
        </thead>
        <tbody>
            <% membersWithDetails.forEach((member, index) => { %>
                <tr class="fadeInRow" style="animation-delay: <%= index * 0.1 %>s;">
                    <td><%= member.name %></td>
                    <td><%= member.email %></td>
                    <td><%= member.memberDetails.age %></td>
                    <td><%= member.memberDetails.mobileNumber %></td>
                    <td><%= new Date(member.memberDetails.joinDate).toLocaleDateString('en-GB', { day: '2-digit', month: 'short', year: 'numeric' }) %></td>
                    <td><%= member.memberDetails.membershipPlan %></td>
                    <td><%= member.memberDetails.planDuration %></td>
                    <td class="amount"><%= member.billDetails.length > 0 ? member.billDetails[0].amount : "N/A" %></td>
                    <td class="status <%= member.billDetails.length > 0 ? member.billDetails[0].status : "N/A" %>"><%= member.billDetails.length > 0 ? member.billDetails[0].status : "N/A" %></td>
                </tr>
            <% }) %>
        </tbody>
    </table>
    <button onclick="window.location.href='/admin/members'">Back to Members</button>
</body>

</html>
