<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        .member-card {
            border: 1px solid #ddd;
            /* Light border */
            border-radius: 8px;
            /* Rounded corners */
            padding: 15px;
            /* Padding inside the card */
            margin: 10px;
            /* Space between cards */
            background-color: #f9f9f9;
            /* Light background */
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            /* Slight shadow for depth */
            transition: transform 0.2s;
            /* Smooth transition for hover effect */
        }

        .member-card:hover {
            transform: scale(1.02);
            /* Slightly grow on hover */
        }

        .member-name {
            font-size: 1.5em;
            /* Larger font for name */
            color: #333;
            /* Darker text color */
            margin-bottom: 10px;
            /* Space below the name */
        }

        strong {
            color: #555;
            /* Slightly darker color for labels */
        }
    </style>
</head>

<body>
    <h1>list of all members here</h1>
    <li><button onclick="window.location.href='/admin/members/bills'">Bills</button></li>
    <li><button onclick="window.location.href='/admin/home'">home</button></li>
    <li><button onclick="window.location.href='/admin/shop'">shop</button></li>

    <% membersWithDetails.forEach(member=> { %>
        <div class="member-card">
            <h3 class="member-name">
                <%= member.name %>
            </h3>
            <p><strong>Email:</strong>
                <%= member.email %>
            </p>
            <p><strong>Age:</strong>
                <%= member.memberDetails.age %>
            </p>
            <p><strong>Mobile Number:</strong>
                <%= member.memberDetails.mobileNumber %>
            </p>
            <p><strong>Joined At:</strong>
                <%= new Date(member.memberDetails.joinDate).toLocaleDateString('en-GB', { day: '2-digit' ,
                    month: 'short' , year: 'numeric' }) %>
            </p>
            <p><strong>Membership Plan:</strong>
                <%= member.memberDetails.membershipPlan %>
            </p>
            <p><strong>Duration:</strong>
                <%= member.memberDetails.planDuration %>
            </p>
            <p><strong>Cart:</strong>
                <%= member.memberDetails.cart %>
            </p>
        </div>
        <% }); %>
</body>

</html>